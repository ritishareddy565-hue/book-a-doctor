import React from "react";

function Navbar() {
  return (
    <div
      style={{
        background: "#007bff",
        padding: "15px",
        color: "white",
      }}
    >
      Home | Doctors | Appointments | Profile | Logout
    </div>
  );
}

export default Navbar;